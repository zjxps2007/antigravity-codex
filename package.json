{
  "name": "antigravity-codex",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Antigravity plugin that delegates reviews and tasks to the local Codex CLI.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests/*.test.mjs",
    "validate": "npm run build && agy plugin validate ."
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
